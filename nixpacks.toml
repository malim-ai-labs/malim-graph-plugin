[phases.install]
cmds = [
    "python3 -m pip install --upgrade pip",
    "python3 -m pip install pymupdf>=1.24 anthropic>=0.40 click>=8.0 pydantic>=2.0 pydantic-settings>=2.0 mcp>=1.0",
    "python3 -m pip install -e .",
]

[start]
cmd = "malimgraph-plugin --transport http --port $PORT"
