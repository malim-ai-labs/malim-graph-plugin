[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = [
    "pip install --upgrade pip",
    "pip install pymupdf>=1.24 anthropic>=0.40 click>=8.0 pydantic>=2.0 pydantic-settings>=2.0 mcp>=1.0",
    "pip install -e .",
]

[start]
cmd = "malimgraph-plugin --transport http --port $PORT"
